import * as utils from 'utils.js';
import HTMLElement from 'HTMLElement.js';

export default class HTMLCanvasElement extends HTMLElement
{
    constructor(){
        super('canvas')

        // var canvas = GameGlobal._screencanvas || wx.createCanvas();
        // GameGlobal._screencanvas = canvas;
        // var wxCanvasElement = canvas.constructor;

        // return wxCanvasElement
    }
};
